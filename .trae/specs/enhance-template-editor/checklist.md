# Checklist

## 后端 AI 生成服务
- [x] `AIGenerateService.ts` 已添加 `generateRace()` 方法
- [x] `AIGenerateService.ts` 已添加 `generateClass()` 方法
- [x] `AIGenerateService.ts` 已添加 `generateBackground()` 方法
- [x] `AIGenerateService.ts` 已添加 `generateWorldSetting()` 方法
- [x] `templateRoutes.ts` 已添加生成路由

## 前端 API 服务
- [x] `templateService.ts` 已添加 `generateRace()` 方法
- [x] `templateService.ts` 已添加 `generateClass()` 方法
- [x] `templateService.ts` 已添加 `generateBackground()` 方法
- [x] `templateService.ts` 已添加 `generateWorldSetting()` 方法

## 属性编辑器
- [x] `AttributeEditor.tsx` 组件已创建
- [x] 支持添加、编辑、删除属性
- [x] `TemplateEditor.tsx` 已添加属性编辑器导航项
- [x] 编辑器组件已正确导出

## 种族编辑器 AI 生成
- [x] `RaceEditor.tsx` 已添加 AI 生成按钮
- [x] AI 生成对话框可正常显示
- [x] 生成结果可预览和接受

## 职业编辑器 AI 生成
- [x] `ClassEditor.tsx` 已添加 AI 生成按钮
- [x] AI 生成对话框可正常显示
- [x] 生成结果可预览和接受

## 背景编辑器 AI 生成
- [x] `BackgroundEditor.tsx` 已添加 AI 生成按钮
- [x] AI 生成对话框可正常显示
- [x] 生成结果可预览和接受

## 世界观编辑器 AI 生成
- [x] `WorldSettingEditor.tsx` 已添加 AI 生成按钮
- [x] AI 生成对话框可正常显示
- [x] 生成结果可预览和接受

## 预览测试功能
- [x] `TemplatePreview.tsx` 组件已创建
- [x] 角色创建流程预览正常
- [x] 初始场景预览正常
- [x] `TemplateEditor.tsx` 已添加预览按钮

## 默认数据
- [x] `templateStore.ts` 已添加 `attributes` 默认值

## 验证
- [x] `pnpm run typecheck` 通过，无类型错误
- [x] 所有新功能可正常使用
